# S2 — Bibliographic citation inventory

`citation_inventory.csv` is the machine-readable bibliographic inventory (176 entries). `role_in_review` is a heuristic from the title/entry type so a reviewer can filter datasets, positioning reviews, and primary studies. It is not a substitute for S3 coding.
