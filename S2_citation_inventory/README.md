# S2 — Bibliographic citation inventory

`citation_inventory.csv` is a machine-readable dump of `references.bib` (176 entries). `role_in_review` is a heuristic from the title/entry type so a reviewer can filter datasets, positioning reviews, and primary studies. It is not a substitute for S3 coding.

The native BibTeX file is copied here as `references.bib`.
