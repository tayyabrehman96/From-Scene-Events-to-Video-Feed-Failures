# Manuscript copy

`main.tex` and `references.bib` here match the compiling copies at the repository root (Springer Nature upload wants figures next to the `.tex` file). Figure PNGs are in `figures/` and also at the repository root.

Working PRISMA values are orange in the PDF while `\provisionalnumberstrue` remains set. See `README_BEFORE_SUBMISSION.md`.
